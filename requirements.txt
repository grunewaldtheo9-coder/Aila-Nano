# Aila Nano — core runtime dependencies. Verified installing cleanly
# (no source builds, no manual fixes) on Python 3.10 through 3.13.
#
# PyTorch is installed separately (CPU or CUDA build) — see docs/INSTALL.md
#   CPU:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch>=2.6 is the first series with official Python 3.13 wheels.
torch>=2.6

# Tokenizer
sentencepiece>=0.2.1

# Vector database / semantic memory (FAISS + Aila Nano's own embeddings)
faiss-cpu>=1.9.0
numpy>=1.26

# Config / utilities
pyyaml>=6.0.2
tqdm>=4.67
tensorboard>=2.18

# Translation (Portuguese <-> English fallback; optional — Aila runs
# without it, just without the translate-on-miss path)
deep-translator>=1.11
